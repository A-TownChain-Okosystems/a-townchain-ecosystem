# atc-blockchain: gemischtes Modul (Python + Rust + TypeScript) — Rust-Crate: kernel/ (dieses Manifest), Python: setup.py, TS: package.json
[package]
name = "atc-blockchain"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "atc_blockchain"
path = "src/blockchain.rs"

[dependencies]
