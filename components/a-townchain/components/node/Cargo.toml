[workspace]

[package]
name = "atc-node"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "ATC Node Runtime — Konfiguration + Peer-Tabelle (MVP)"

[dependencies]
atc-algorithm = { path = "../../../atc-algorithm" }  # canonical local workspace source
serde = { version = "1", features = ["derive"] }
serde_json = "1"
