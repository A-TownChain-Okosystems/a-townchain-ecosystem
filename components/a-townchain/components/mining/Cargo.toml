[package]
name = "atc-mining"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "ATC Mining — execution and deterministic mining primitives; consensus remains canonical in atc-algorithm"

[dependencies]
sha3 = "0.10"
atc-algorithm = { path = "../../../atc-algorithm" }

# This component is intentionally a standalone migrated crate. It is not a
# member of the root workspace because the root already contains the canonical
# atc-mining package.
[workspace]
