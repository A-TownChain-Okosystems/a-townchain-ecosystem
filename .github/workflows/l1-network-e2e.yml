name: L1 Network E2E
# CI trigger: wallet/node/kernel integration changes must execute the full L1 gate.

on:
  push:
    branches:
      - main
      - feat/atc-wallet-rust-l1-integration
  pull_request:
    paths:
      - "components/atc-wallet/**"
      - "components/atc-node/**"
      - "components/a-townchain/modules/atc-blockchain/**"
      - ".github/workflows/l1-network-e2e.yml"

permissions:
  contents: read

jobs:
  tx-block-reward-state-finality-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Cargo test — tx → block → reward → state → finality → recovery
        run: cargo test --manifest-path components/atc-node/Cargo.toml --test l1_transaction_lifecycle_e2e -- --nocapture

  wallet-node-process-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Cargo test — wallet → atc-node → mempool → block → state
        run: cargo test --manifest-path components/atc-node/Cargo.toml --test wallet_node_process_e2e -- --nocapture

  persistent-atc-node-multi-process-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Cargo test — persistent atc-node A/B consensus
        run: cargo test --manifest-path components/atc-node/Cargo.toml --test persistent_multi_node_consensus_e2e -- --nocapture

  multi-process-network-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Cargo test — canonical multi-process network E2E
        run: cargo test --manifest-path components/a-townchain/modules/atc-blockchain/kernel/Cargo.toml --test multi_process_network_e2e -- --nocapture
